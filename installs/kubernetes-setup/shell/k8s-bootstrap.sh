
  #yum install epel-release -y
  #yum install ansible -y

  #cat devops/hosts >>/etc/ansible/hosts
  #mkdir /etc/ansible/group_vars
  #echo "ansible_ssh_user: vagrant" >> servers

  echo "k8s host provisioning is  completed................................................"
